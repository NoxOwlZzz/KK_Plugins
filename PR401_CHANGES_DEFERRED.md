# PR #401 changes deferred

> **Historical scope record.** The deferral described below has ended. The
> current branch removes Basic/Advanced and implements optional, presentation-
> only `Category > Subcategory > Property` organization. Refer to
> `MANIFEST_SCHEMA_V2.md` for the maintained contract.

This branch intentionally does not implement, answer, push or update changes
requested by reviewers of PR #401.

Deferred to a later separately authorized task:

- `SubCategory` and any related manifest schema;
- removal or redesign of `UiLevel`;
- removal or redesign of Basic/Advanced;
- reviewer-requested API or compatibility changes;
- reviewer replies, force-pushes or PR synchronization.

The current phase is limited to correcting the already-redesigned UI,
stability, interaction and measured performance while preserving existing
manifest, persistence, MaterialAPI and Extension API behavior.
